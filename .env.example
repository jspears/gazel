# Bazel workspace configuration
# Set this to the root directory of your Bazel workspace
# This should be the directory containing your MODULE.bazel file

BAZEL_WORKSPACE=/path/to/your/bazel/workspace

# Optional: Server port configuration (defaults to 3001)
# PORT=3001
